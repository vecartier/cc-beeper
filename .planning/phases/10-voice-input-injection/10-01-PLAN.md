---
phase: 10-voice-input-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/VoiceService.swift
  - Sources/ClaudeMonitor.swift
  - Sources/ContentView.swift
autonomous: false
requirements: [VOICE-01, VOICE-02, INFRA-04]

must_haves:
  truths:
    - "User presses Speak (or Option+S) and recording starts with SFSpeechRecognizer on-device"
    - "User presses Speak (or Option+S) again and recording stops, transcription is injected into terminal"
    - "After injection + Enter, user's previous app is refocused within 500ms"
    - "User never sees the terminal — focus switch + inject + refocus happens invisibly"
    - "Voice input works from any app (Figma, Chrome, etc.) across all Claude Code sessions"
    - "Audio engine handles headphone changes by recreating AVAudioEngine each session"
  artifacts:
    - path: "Sources/VoiceService.swift"
      provides: "Voice recording, transcription, CGEvent injection, terminal focus/refocus"
      exports: ["VoiceService"]
    - path: "Sources/ClaudeMonitor.swift"
      provides: "VoiceService ownership and isRecording binding"
      contains: "voiceService"
    - path: "Sources/ContentView.swift"
      provides: "Speak button wired to VoiceService.toggle()"
      contains: "voiceService"
  key_links:
    - from: "Sources/ContentView.swift"
      to: "Sources/VoiceService.swift"
      via: "monitor.voiceService.toggle() on Speak button tap"
      pattern: "voiceService\\.toggle"
    - from: "Sources/ClaudeMonitor.swift"
      to: "Sources/VoiceService.swift"
      via: "ClaudeMonitor owns VoiceService, syncs isRecording"
      pattern: "let voiceService.*VoiceService"
    - from: "Sources/VoiceService.swift"
      to: "CGEvent HID injection"
      via: "injectAndSubmit posts keyDown/keyUp via .cghidEventTap"
      pattern: "post\\(tap: \\.cghidEventTap\\)"
---

<objective>
Wire real voice recording into Claumagotchi's Speak button. When user presses Speak (or Option+S), record via SFSpeechRecognizer, transcribe on-device, inject the text into the terminal via CGEvent HID, press Enter, and refocus the previous app. The user never sees the terminal switch.

Purpose: This is the core voice input pipeline — the user speaks from any app and the message lands in Claude Code without manual typing or terminal switching.
Output: VoiceService.swift (new), updated ClaudeMonitor.swift and ContentView.swift
</objective>

<execution_context>
@/Users/vcartier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vcartier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-voice-input-injection/10-CONTEXT.md

<interfaces>
<!-- Working prototype code to copy from — DO NOT reinvent -->
From /Users/vcartier/Desktop/VoiceLoop/Sources/LoopEngine.swift:
- startRecording() — lines 168-246: SFSpeechRecognizer setup, AVAudioEngine with format:nil, installTap bufferSize:4096, permission checks
- stopRecording() — lines 248-266: removeTap, endAudio, cancel task, recreate AVAudioEngine, call injectAndSubmit
- injectAndSubmit() — lines 270-307: focusTerminal, CGEvent HID injection (<=200 UTF-16 chars) or clipboard paste fallback, press Enter
- focusTerminal() — lines 311-331: Process + /usr/bin/open -a for terminal apps
- toggleRecording() — lines 159-166: if recording stop else (if speaking stopSpeaking then) startRecording

From Sources/ClaudeMonitor.swift:
```swift
@Published var isRecording: Bool = false  // line 89 — stub, drives UI
// handleHotKey case 1 (Option+S) — line 448-449: currently just toggles isRecording
```

From Sources/ContentView.swift:
```swift
// Speak button action — line 176: monitor.isRecording.toggle()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VoiceService with recording, transcription, and injection</name>
  <files>Sources/VoiceService.swift</files>
  <read_first>
    /Users/vcartier/Desktop/VoiceLoop/Sources/LoopEngine.swift
    Sources/ClaudeMonitor.swift
  </read_first>
  <action>
Create `Sources/VoiceService.swift` as a `final class VoiceService: ObservableObject, @unchecked Sendable`.

Copy and adapt the following from LoopEngine.swift (lines 157-331):

**Properties:**
- `@Published var isRecording: Bool = false`
- `@Published var lastTranscriptPreview: String = ""` (for potential screen display)
- `private var audioEngine = AVAudioEngine()`
- `private var recognitionRequest: SFSpeechAudioBufferRecognitionRequest?`
- `private var recognitionTask: SFSpeechRecognitionTask?`
- `private let recognizer = SFSpeechRecognizer(locale: Locale(identifier: "en-US"))`
- `private var lastTranscript: String = ""`
- `private var previousAppPID: pid_t? = nil` (capture BEFORE focusing terminal)

**Methods — copy logic directly from LoopEngine.swift:**

1. `func toggle()` — If recording, call stopRecording(). Else call startRecording(). This is the public API called by hotkey and button.

2. `func stopIfRecording()` — Public convenience: `if isRecording { stopRecording() }`. Called by ClaudeMonitor on power-off.

3. `private func startRecording()` — Copy from LoopEngine lines 168-246 exactly:
   - Check SFSpeechRecognizer.authorizationStatus(), request if .notDetermined, guard .authorized
   - Check AVCaptureDevice.authorizationStatus(for: .audio), request if .notDetermined, guard .authorized
   - If already recording, call stopRecording() first
   - Create SFSpeechAudioBufferRecognitionRequest with requiresOnDeviceRecognition=true, shouldReportPartialResults=true
   - Recreate AVAudioEngine() (do NOT reuse — per CONTEXT.md decision)
   - Get inputNode, check nativeFormat.sampleRate > 0 and channelCount > 0
   - installTap with bufferSize 4096, format: nil (handles headphones per CONTEXT.md)
   - Create recognitionTask that updates lastTranscript on main thread from bestTranscription.formattedString
   - Ignore error code 216 (cancelled — expected on stop)
   - audioEngine.prepare() then try audioEngine.start()
   - Set isRecording = true on success
   - **ADDITION not in prototype:** Before starting, capture `previousAppPID = NSWorkspace.shared.frontmostApplication?.processIdentifier`

4. `private func stopRecording()` — Copy from LoopEngine lines 248-266:
   - Guard isRecording
   - audioEngine.inputNode.removeTap(onBus: 0)
   - audioEngine.stop()
   - recognitionRequest?.endAudio(), set nil
   - recognitionTask?.cancel(), set nil
   - audioEngine = AVAudioEngine() (recreate per CONTEXT.md)
   - isRecording = false
   - Capture lastTranscript, clear it
   - Update lastTranscriptPreview on main thread
   - If text not empty, call injectAndSubmit(text)

5. `private func injectAndSubmit(_ text: String)` — Copy from LoopEngine lines 270-307:
   - Guard not empty
   - Call focusTerminal()
   - usleep(500_000) — wait for terminal to come forward
   - Get Array(text.utf16)
   - If utf16.count <= 200: CGEvent with nil source, virtualKey 0, keyDown true/false. Call keyboardSetUnicodeString on BOTH keyDown AND keyUp. Post via .cghidEventTap
   - If utf16.count > 200: clipboard paste fallback — save old pasteboard, set text, Cmd+V (virtualKey 9 with .maskCommand), usleep(100_000), restore old pasteboard
   - usleep(100_000) then press Enter: CGEvent virtualKey 0x24 keyDown/keyUp, post via .cghidEventTap
   - After Enter, call refocusPreviousApp()

6. `private func focusTerminal()` — Copy from LoopEngine lines 311-331:
   - Same terminal bundle ID list as ClaudeMonitor.activateTerminal(): com.apple.Terminal, com.googlecode.iterm2, dev.warp.Warp-Stable, io.alacritty, net.kovidgoyal.kitty, com.github.wez.wezterm
   - For matching running app, use Process() with launchPath "/usr/bin/open" and arguments ["-a", matchName]
   - try? task.run()

7. `private func refocusPreviousApp()` — NEW, not in prototype:
   - Guard let pid = previousAppPID
   - usleep(200_000) — brief pause after Enter
   - If let app = NSRunningApplication(processIdentifier: pid): app.activate()
   - Clear previousAppPID

**Logging:** Add a private log() method writing to "/tmp/claumagotchi-voice.log" (same pattern as LoopEngine line 26-36) for debugging.

**Import:** Foundation, AVFoundation, Speech, AppKit, ApplicationServices

**Do NOT include:** TTS/speaking code (that's Phase 11), file watcher code, summarization code. Only recording + injection.
  </action>
  <verify>
    <automated>grep -c "func toggle\|func stopIfRecording\|func startRecording\|func stopRecording\|func injectAndSubmit\|func focusTerminal\|func refocusPreviousApp" Sources/VoiceService.swift | grep -q "7"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "final class VoiceService: ObservableObject" Sources/VoiceService.swift` returns a match
    - `grep "requiresOnDeviceRecognition = true" Sources/VoiceService.swift` returns a match
    - `grep "shouldReportPartialResults = true" Sources/VoiceService.swift` returns a match
    - `grep "format: nil" Sources/VoiceService.swift` returns a match (not a specific audio format)
    - `grep "bufferSize: 4096" Sources/VoiceService.swift` returns a match
    - `grep "CGEvent(keyboardEventSource: nil" Sources/VoiceService.swift` returns a match (nil source per CONTEXT.md)
    - `grep "post(tap: .cghidEventTap)" Sources/VoiceService.swift` returns a match
    - `grep "keyboardSetUnicodeString" Sources/VoiceService.swift` returns at least 2 matches (keyDown AND keyUp)
    - `grep "virtualKey: 0x24" Sources/VoiceService.swift` returns a match (Enter key)
    - `grep "previousAppPID" Sources/VoiceService.swift` returns a match
    - `grep "NSRunningApplication(processIdentifier:" Sources/VoiceService.swift` returns a match
    - `grep "audioEngine = AVAudioEngine()" Sources/VoiceService.swift` returns at least 2 matches (init + recreate in stopRecording)
    - `grep "/usr/bin/open" Sources/VoiceService.swift` returns a match
    - No import of FoundationModels (that's Phase 11)
  </acceptance_criteria>
  <done>VoiceService.swift exists with all 7 methods, uses on-device SFSpeechRecognizer, CGEvent HID injection with nil source, clipboard fallback for long text, Enter key submission, terminal focus via open -a, and previous app refocus via NSRunningApplication.</done>
</task>

<task type="auto">
  <name>Task 2: Wire VoiceService into ClaudeMonitor and ContentView</name>
  <files>Sources/ClaudeMonitor.swift, Sources/ContentView.swift</files>
  <read_first>
    Sources/VoiceService.swift
    Sources/ClaudeMonitor.swift
    Sources/ContentView.swift
    Sources/ClaumagotchiApp.swift
  </read_first>
  <action>
**ClaudeMonitor.swift changes:**

1. Add property: `let voiceService = VoiceService()` (after the @Published properties block, around line 95)

2. Add a Combine subscription to sync VoiceService.isRecording -> ClaudeMonitor.isRecording. In init(), after the existing setup code (after line 136), add:
   ```swift
   voiceService.$isRecording
       .receive(on: DispatchQueue.main)
       .assign(to: &$isRecording)
   ```
   This replaces the manual `isRecording` toggle — ClaudeMonitor.isRecording becomes a read-only mirror of VoiceService.isRecording.

3. Remove the `@Published` from `isRecording` and make it `@Published private(set) var isRecording: Bool = false` — it should only be set by the Combine pipeline from VoiceService, never directly.

4. Update handleHotKey case 1 (Option+S, line 448-449): Replace `self.isRecording.toggle()` with `self.voiceService.toggle()`.

5. In the isActive didSet (line 66-77), when isActive becomes false, also stop any active recording: add `voiceService.stopIfRecording()` before the existing teardown. This ensures powering off stops recording cleanly.

6. Add a Combine import if not already present (it is — line 2).

**ContentView.swift changes:**

1. Update the Speak button action (line 175-177): Replace `monitor.isRecording.toggle()` with `monitor.voiceService.toggle()`.

**Build verification:** Run xcodebuild from the project root to confirm compilation succeeds.
  </action>
  <verify>
    <automated>cd /Users/vcartier/Desktop/Claumagotchi && xcodebuild -scheme Claumagotchi -destination 'platform=macOS' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "let voiceService = VoiceService()" Sources/ClaudeMonitor.swift` returns a match
    - `grep "voiceService.toggle()" Sources/ClaudeMonitor.swift` returns a match (in handleHotKey)
    - `grep "voiceService.toggle()" Sources/ContentView.swift` returns a match (in Speak button)
    - `grep "voiceService.\$isRecording" Sources/ClaudeMonitor.swift` returns a match (Combine binding)
    - `grep "private(set) var isRecording" Sources/ClaudeMonitor.swift` returns a match
    - `grep "voiceService.stopIfRecording()" Sources/ClaudeMonitor.swift` returns a match (in isActive didSet)
    - The project builds without errors
  </acceptance_criteria>
  <done>ClaudeMonitor owns VoiceService, hotkey Option+S calls voiceService.toggle(), Speak button calls voiceService.toggle(), isRecording is synced via Combine, power-off stops recording, project compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify voice input end-to-end</name>
  <files>Sources/VoiceService.swift, Sources/ClaudeMonitor.swift, Sources/ContentView.swift</files>
  <action>
Human verification of the complete voice input pipeline. No code changes — this task validates the work from Tasks 1 and 2 by running the app and testing the full flow.
  </action>
  <what-built>Complete voice input pipeline: press Speak (or Option+S) to record, press again to stop, transcription is injected into the terminal and Enter is pressed, previous app is refocused.</what-built>
  <how-to-verify>
    1. Build and run the app (Cmd+R in Xcode)
    2. Grant microphone and speech recognition permissions when prompted
    3. Ensure Accessibility permission is granted (System Settings > Privacy > Accessibility > Claumagotchi)
    4. Open a Claude Code session in Terminal
    5. Switch to a different app (e.g., Finder or Safari)
    6. Press Option+S — the Speak button should turn red with a pulse
    7. Say something like "list all files in the current directory"
    8. Press Option+S again — button should return to normal
    9. Verify: the text appeared in the terminal and Enter was pressed (Claude started processing)
    10. Verify: you were refocused back to the app you were in (Finder/Safari) — you should NOT see the terminal
    11. Try with a long sentence (over 200 characters) to test the clipboard paste fallback
    12. Try clicking the Speak button on the egg (instead of hotkey) — should work the same
    13. Power off from menu bar — verify recording stops if active
  </how-to-verify>
  <verify>User confirms all 13 verification steps pass</verify>
  <acceptance_criteria>
    - Voice recording starts on Option+S or Speak button tap
    - Voice recording stops on second press
    - Transcribed text appears in terminal
    - Enter is pressed automatically after injection
    - Previous app is refocused after injection (user does not see terminal)
    - Clipboard paste fallback works for long text
    - Power-off stops active recording
  </acceptance_criteria>
  <done>User has confirmed end-to-end voice input works: record, transcribe, inject, submit, refocus.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- VoiceService.swift exists with recording, transcription, injection, and refocus logic
- ClaudeMonitor.swift owns VoiceService and syncs isRecording via Combine
- ContentView.swift Speak button calls voiceService.toggle()
- Option+S hotkey triggers voiceService.toggle()
- Project builds without errors
- End-to-end: speak -> transcribe -> inject -> Enter -> refocus works from any app
</verification>

<success_criteria>
1. Pressing Speak button (or Option+S) starts recording; pressing again stops and submits
2. Transcribed text is injected into the terminal via CGEvent HID and Enter is pressed
3. After injection, the previous app is refocused within 500ms — user never sees the terminal
4. Voice input works while the user is in any app (Figma, Chrome, etc.)
5. Works across all projects and Claude Code sessions (uses terminal focus, not session-specific)
6. Audio engine handles headphone changes (format: nil) and recovers from errors (recreate engine each session)
</success_criteria>

<output>
After completion, create `.planning/phases/10-voice-input-injection/10-01-SUMMARY.md`
</output>
