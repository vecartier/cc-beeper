---
phase: 04-notifications
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - Sources/ClaumagotchiApp.swift
autonomous: false
requirements:
  - NOTIF-04

must_haves:
  truths:
    - "The menu bar includes a toggle button for enabling/disabling notifications"
    - "Toggling the button changes the notificationsEnabled state immediately"
    - "The toggle label reflects the current state (Disable/Enable)"
    - "The preference persists across app restarts"
  artifacts:
    - path: "Sources/ClaumagotchiApp.swift"
      provides: "Notifications toggle button in MenuBarExtra"
      contains: "Disable Notifications"
  key_links:
    - from: "Sources/ClaumagotchiApp.swift"
      to: "Sources/ClaudeMonitor.swift"
      via: "monitor.notificationsEnabled.toggle()"
      pattern: "notificationsEnabled"
---

<objective>
Add a notifications toggle button to the MenuBarExtra menu and verify the complete notification system works end-to-end.

Purpose: Give users control over notification delivery via the same menu bar they already use for sounds and YOLO mode (NOTIF-04).

Output: Modified ClaumagotchiApp.swift with notifications toggle button.
</objective>

<execution_context>
@/Users/vcartier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vcartier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-notifications/04-01-SUMMARY.md

@Sources/ClaumagotchiApp.swift
@Sources/ClaudeMonitor.swift

<interfaces>
<!-- From ClaumagotchiApp.swift — existing toggle pattern to mirror -->
```swift
// Sound toggle button (line ~43-46) — clone this pattern for notifications:
Button(monitor.soundEnabled ? "Disable Sounds" : "Enable Sounds") {
    monitor.soundEnabled.toggle()
}
.keyboardShortcut("s")
```

<!-- From ClaudeMonitor.swift (after Plan 01) — property to bind to -->
```swift
@Published var notificationsEnabled: Bool {
    didSet { UserDefaults.standard.set(notificationsEnabled, forKey: "notificationsEnabled") }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add notifications toggle to MenuBarExtra</name>
  <read_first>
    - Sources/ClaumagotchiApp.swift (current MenuBarExtra content — find exact position after sound toggle)
    - Sources/ClaudeMonitor.swift (verify notificationsEnabled property exists from Plan 01)
  </read_first>
  <files>Sources/ClaumagotchiApp.swift</files>
  <action>
In `ClaumagotchiApp.swift`, inside the `MenuBarExtra` content closure, add a notifications toggle button immediately AFTER the existing sound toggle button (after the `.keyboardShortcut("s")` line):

```swift
Button(monitor.notificationsEnabled ? "Disable Notifications" : "Enable Notifications") {
    monitor.notificationsEnabled.toggle()
}
.keyboardShortcut("n")
```

This mirrors the exact pattern used by the sound toggle and YOLO mode toggle. The "n" keyboard shortcut does not conflict with any existing shortcut in the menu (existing: "a", "d", "g", "s", Cmd+Shift+A, Cmd+Shift+H, "q").
  </action>
  <verify>
    <automated>cd /Users/vcartier/Desktop/Claumagotchi && swift build 2>&1 | tail -5 && grep -c "Disable Notifications" Sources/ClaumagotchiApp.swift && grep -c "notificationsEnabled" Sources/ClaumagotchiApp.swift</automated>
  </verify>
  <acceptance_criteria>
    - ClaumagotchiApp.swift contains a Button with label "Disable Notifications" / "Enable Notifications"
    - The button action calls `monitor.notificationsEnabled.toggle()`
    - The button has `.keyboardShortcut("n")`
    - The button is positioned after the sound toggle and before the Theme menu
    - `swift build` succeeds with no errors
  </acceptance_criteria>
  <done>Notifications toggle button appears in menu bar with keyboard shortcut N, mirroring the sound toggle pattern</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete notification system end-to-end</name>
  <files>Sources/ClaumagotchiApp.swift, Sources/ClaudeMonitor.swift, Sources/NotificationManager.swift, build.sh</files>
  <action>
Build and launch the app, then verify the complete notification subsystem works:
1. Run `./build.sh` to build and sign the app
2. Launch Claumagotchi.app
3. Test the menu bar toggle
4. Test notification delivery for all event types
  </action>
  <what-built>Complete macOS Notification Center integration: NotificationManager with foreground delivery delegate, notification calls at all three event sites (permission requests, session completion, tool errors/timeouts), notifications toggle in menu bar, ad-hoc code signing in build.sh</what-built>
  <how-to-verify>
    1. Run `./build.sh` to build and sign the app
    2. Launch `Claumagotchi.app` — on first launch, macOS should show a notification permission dialog. Allow it.
    3. Open the menu bar egg icon — verify "Disable Notifications" appears between the sound toggle and Theme menu
    4. Click "Disable Notifications" — verify it changes to "Enable Notifications"
    5. Quit and relaunch — verify the toggle remembers its state
    6. Set it back to "Disable Notifications" (enabled)
    7. Trigger a Claude session and wait for it to complete — verify a "Claude Is Done" notification banner appears
    8. If possible, trigger a permission request — verify a "Claude Needs Your Permission" notification banner appears
    9. Verify the notification appears as a macOS Notification Center banner (top-right), not just in the notification history
  </how-to-verify>
  <verify>Manual verification — user confirms notification banners appear</verify>
  <done>All four NOTIF requirements confirmed working: permission notifications, session done notifications, error/timeout notifications, and toggle persistence</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `swift build` succeeds
2. `grep "Disable Notifications" Sources/ClaumagotchiApp.swift` confirms toggle button
3. `grep "notificationsEnabled" Sources/ClaumagotchiApp.swift` confirms binding
4. Manual smoke test: launch app, verify notification permission dialog, verify toggle, verify banner delivery
</verification>

<success_criteria>
- Menu bar shows notifications toggle that persists across restarts
- Notification banners appear in macOS Notification Center for permission requests, session completions, and tool errors
- Toggle disables/enables all notification delivery
- App is code-signed and notification permission dialog appears on first launch
</success_criteria>

<output>
After completion, create `.planning/phases/04-notifications/04-02-SUMMARY.md`
</output>
